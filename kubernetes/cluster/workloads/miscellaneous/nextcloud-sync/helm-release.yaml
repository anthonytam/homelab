apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nextcloud-sync
  namespace: miscellaneous
spec:
  interval: 1h
  chart:
    spec:
      chart: app-template
      version: 5.1.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      interval: 1h
  values:
    defaultPodOptions:
      imagePullSecrets:
       - name: ghcr-image-pull-secret
    controllers:
      dmc:
        enabled: true
        type: cronjob
        cronjob:
          schedule: "3 3 * * *"
        containers:
          dmc:
            image:
              repository: ghcr.io/anthonytam/nextcloud-sync
              tag: "20260421"
            env:
              MY_UID: 33
              MY_GID: 33
            envFrom:
              - secret: nextcloud-sync-configuration
            probes:
              liveness:
                enabled: false
              readiness:
                enabled: false
              startup:
                enabled: false
    persistence:
      dmc:
        enabled: true
        type: nfs
        server: truenas.local.tam.land
        path: /mnt/main-bay/k8s-data/miscellaneous/nextcloud-sync/dmc
        advancedMounts:
          dmc:
            dmc:
              - path: /nextcloud-data
