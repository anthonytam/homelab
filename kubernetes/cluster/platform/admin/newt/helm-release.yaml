apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: newt
  namespace: admin
spec:
  interval: 1h
  chart:
    spec:
      chart: app-template
      version: 4.6.3
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      interval: 1h
  values:
    controllers:
      app:
        enabled: true
        containers:
          app:
            image:
              repository: fosrl/newt
              tag: 1.15.0
            envFrom:
              - secret: newt-configuration