apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-magento-mariadb-pvc
  namespace: homerow
spec:
  resources:
    requests:
      storage: 10Gi
  accessModes:
  - ReadWriteOnce
  storageClassName: "local-path"